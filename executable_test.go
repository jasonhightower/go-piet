package main

import "testing"

func ExecutableTest(t *testing.T) {
    /*
    main := InstBlock{Label:"main"}
    main.Instructions = []Instr{
        {Op: Push, Data: 1},
        {Op: Dup},
        {Op: Add},
        {Op: Exit},
    }
    exec := Executable{}
    exec.Blocks = make(map[string]InstBlock)
    exec.Blocks["main"] = main

    stack := NewStack(10)
    Run(exec, stack)
    */
}
